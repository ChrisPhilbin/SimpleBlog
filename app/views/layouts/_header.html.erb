    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to "Chris Philbin's Blog", root_url, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
          <% if logged_in? %>
            <%= link_to "Submit new", new_article_path %>
          <% else %>
            <%= link_to "Log in", login_path %>
          <% end %>
          </ul>
        </nav>
      </div>
    </header>